I have this: "  RAW_DATA.result.forEach((item, index) => {

    const estTimeCreation = new Date(item['Created Date/Time']);
    const creationUTC = converterUTC(estTimeCreation, parseInt(item['GMT Offset']));


    RAW_DATA.result[index]['Created Date/Time'] = creationUTC;

    if(item['Original Lead Owner Assignment Date/Time']){

      //GET EST TIME
      const estTimeAssignment = new Date(item['Original Lead Owner Assignment Date/Time']);

      const assignmentUTC = converterUTC(estTimeAssignment, parseInt(item['GMT Offset']));
      
      RAW_DATA.result[index]['Original Lead Owner Assignment Date/Time'] = assignmentUTC;

    }
    
  })

function converterUTC(date, GMTOffset) {
  const GMTMinutes = GMTOffset * 60;
  const utcMilliseconds = Date.UTC(
    date.getFullYear(),
    date.getMonth(),
    date.getDate(),
    date.getHours(),
    date.getMinutes() + (GMTMinutes), //CONVERT TO MINUTES
    date.getSeconds()
  );
  date = new Date(utcMilliseconds);
  return (
    date.getMonth() + 1 + "/" +
    date.getDate() + "/" +
    date.getFullYear() + " " +
    date.toLocaleString('en-US', { hour: 'numeric', minute: 'numeric', hour12: true })
  );
}" and I need to know if I am converting to UTC correctly to then  sum GMTOffset negative value the minutes  
